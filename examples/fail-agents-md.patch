diff --git a/src/core/paths.ts b/src/core/paths.ts
new file mode 100644
--- /dev/null
+++ b/src/core/paths.ts
@@ -0,0 +1,5 @@
+import { loadCodexContext } from "../codex/adapter.js";
+
+export function repoAgents(root: string): string | undefined {
+  return loadCodexContext(root).agentsMd;
+}
