diff --git a/src/core/paths.ts b/src/core/paths.ts
new file mode 100644
--- /dev/null
+++ b/src/core/paths.ts
@@ -0,0 +1,5 @@
+import { isTestFile } from "./gate.js";
+
+export function isCoreFile(path: string): boolean {
+  return path.startsWith("src/core/") && !isTestFile(path);
+}
