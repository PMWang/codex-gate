<!-- codex-gate dogfoods itself: this description must match your diff. -->

## What changed

## Why

## Verification
<!-- Paste the command you actually ran. Don't claim "tests pass" if you didn't run them. -->

- [ ] `npm run dev -- run --staged` is clean
